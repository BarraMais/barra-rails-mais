<%= form_for(photo) do |f| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image , "image"%>
    <%= f.file_field :image, :multiple =>:true %>  </br>
  </div>

  <div class="field">
    <%= f.label :image , "image"%>
    <%= f.file_field :image, :multiple =>:true %>  </br>
  </div>

  <div class="field">
    <%= f.hidden_field :album_id, :value => '1' %>  </br>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
